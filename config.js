/* Dream Walker's Atlas — config.js
   Where the Atlas's Supabase project lives. Fill in both values from your
   project's Connect dialog (or Settings > API Keys).

   Both are safe to publish. The publishable key only lets a page do what the
   database rules allow, and those rules are in supabase/schema.sql.
   NEVER put a secret key (sb_secret_...) in this file or anywhere on the site. */
var DW_CONFIG={
  supabaseUrl:"https://ryxmrkwgdyphcdlglwka.supabase.co",
  supabaseKey:"sb_publishable_rsf04iH4Pph9ZfLt8g6Ovg_EI_Zk9GY"
};
